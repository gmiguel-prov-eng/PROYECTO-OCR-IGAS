# Notebooks originales

Esta carpeta se usa localmente para conservar los notebooks originales como respaldo historico.

Los notebooks no se versionan porque pueden contener rutas, salidas ejecutadas o datos sensibles. La logica operativa del proyecto vive en `src/`.
